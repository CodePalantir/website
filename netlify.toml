[build]
  command = "npm install --no-audit --no-fund && npm run build:css && bundle exec jekyll build"
  publish = "_site"

[build.environment]
  JEKYLL_ENV = "production"
  NODE_VERSION = "20"

# /blog moved to /articles (old site URLs stay valid)
[[redirects]]
  from = "/blog"
  to = "/articles"
  status = 301

[[redirects]]
  from = "/blog/*"
  to = "/articles/:splat"
  status = 301
